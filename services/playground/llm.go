package domain

/*
import (
	"context"

	"github.com/company/infragpt"
)

// LLMService defines the interface for LLM integration
type LLMService interface {
	// AnalyzeRequest processes a natural language request and extracts structured information
	AnalyzeRequest(ctx context.Context, text, requesterID string) (*infragpt.Request, error)

	// GenerateTerraformChanges creates Terraform code for the requested changes
	GenerateTerraformChanges(ctx context.Context, req *infragpt.Request, state *infragpt.ResourceState) (*infragpt.TerraformChanges, error)

	// ExplainChanges generates a human-readable explanation of the proposed changes
	ExplainChanges(ctx context.Context, req *infragpt.Request, changes *infragpt.TerraformChanges) (string, error)
}

*/
