package domain

/*
import (
	"context"

	"github.com/company/infragpt"
)

// GitHubService defines the interface for github integration
type GitHubService interface {
	// EnsureRepository ensures the IAC repository exists
	EnsureRepository(ctx context.Context) error

	// GetFileContent retrieves file content from the repository
	GetFileContent(ctx context.Context, path, branch string) (string, error)

	// CreatePullRequest creates a new pull request with Terraform changes
	CreatePullRequest(ctx context.Context, req *infragpt.Request, changes *infragpt.TerraformChanges) (*infragpt.PullRequestDetails, error)

	// GetPullRequestStatus checks the status of a pull request
	GetPullRequestStatus(ctx context.Context, prNumber int) (string, error)

	// MergePullRequest merges an approved pull request
	MergePullRequest(ctx context.Context, prNumber int) error
}

*/
